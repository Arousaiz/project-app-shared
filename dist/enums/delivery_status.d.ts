export declare enum DeliveryStatus {
    AWAITING_CONFIRMATION = "awaiting confirmation",
    IN_TRANSIT = "in transit",
    DELIVERED = "delivered",
    CANCELLED = "cancelled",
    RETURNED = "returned"
}
