{
  "name": "@arousaiz/project-app-shared",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc && npm run package",
    "package": "@powershell copy package.json dist/ && cd dist && npm pack"
  },
  "author": "arousaiz",
  "license": "ISC",
  "dependencies": {
    "typeorm": "^0.3.22"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  },
  "description": "test package"
}